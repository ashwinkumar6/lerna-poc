name: github release and npm publish
on:
  push:
    branches:
      - "main"
jobs:
  release:
    environment: release
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "environment secret: ${{ secrets.NPM_TOKEN }}"
          echo "environment variable: ${{ env.ENVIRONMENT_NAME }}"
